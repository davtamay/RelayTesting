var should = require('should');

const syncServer = require('../sync');

describe("Sync Server Unit Tests", function (done) {
    it("should have 0 sessions on startup", function () {
        
    });
});